void setup() {
}

void draw() {
    var width = window.innerWidth;
    var height = window.innerHeight;
    size(width, height);
    background(100);
    stroke(255);
    ellipse(width/2, height/2, 25, 25);
}
